<Window x:Class="ChatManager.Views.MessageBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChatManager.Views"
        xmlns:vm="clr-namespace:ChatManager.ViewModels"
        Title="{Binding Title}" WindowStartupLocation="CenterOwner"
        Height="150" Width="300" ResizeMode="NoResize" WindowStyle="ToolWindow">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
        <TextBlock Text="{Binding Message}" TextAlignment="Center" Margin="10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="{Binding ButtonOneText}" Command="{Binding ButtonOneCommand}" Width="70" Margin="5"/>
            <Button Content="{Binding ButtonTwoText}" Command="{Binding ButtonTwoCommand}" Visibility="{Binding ButtonTwoVisibility}" Width="70" Margin="5"/>
        </StackPanel>
    </StackPanel>
</Window>
